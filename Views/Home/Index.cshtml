@{
    ViewData["Title"] = "Home";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <h4>to Film Manager</h4>
</div>

@if (User.Identity.IsAuthenticated)
{
    @if (User.IsInRole("Admin"))
    {
        <h2>Admin</h2>
    }
    else
    {
        <h2>Guest</h2>
    }
}
